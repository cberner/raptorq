language: rust
rust:
  - stable
before_script:
  - cargo install cargo-lichking
  - rustup component add rustfmt
  - rustup component add clippy
  - rustup install nightly --profile minimal -y
  - rustup override set --path python nightly
script:
  - make test
  - make build-py
